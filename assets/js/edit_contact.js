$(document).ready(function () {    let inputs = document.querySelectorAll('.edit-contact-bottom input')    for (let input of inputs) {        if(input.value) {            input.classList.add('value')        }    }    $('.edit-contact-bottom input').change(function () {        if ($(this).val()) {            $(this).addClass('value')        } else {            $(this).removeClass('value')        }    })    // input clear when click clear button    $('.input-clean-div svg').click(function () {        let new_contact_input = $(this).parent().parent().children('.input-div').children().children('input');        new_contact_input.val("")    })    // from person    const close_person_btn = $('#close-person-btn');    const person_btn_div = $('.person-btn-div');    const to_contact_div = $('.to-contact-div')    const person_bottom = $('.person-bottom')    const active_edit_btn = $('#active-edit-btn')    // from edit    const close_edit_btn = $('#close-edit-btn');    const edit_contact_img_btn = $('#edit-contact-img-btn')    const edit_contact_save_btn = $('#edit-contact-save-btn')    const edit_contact_bottom = $('.edit-contact-bottom')    // person contact    active_edit_btn.click(() => {        activeEdit();    })    function activeEdit() {        close_person_btn.addClass('d-none')        person_btn_div.addClass('d-none')        to_contact_div.addClass('d-none')        person_bottom.addClass('d-none')        close_edit_btn.removeClass('d-none')        edit_contact_img_btn.removeClass('d-none')        edit_contact_save_btn.removeClass('d-none')        edit_contact_bottom.removeClass('d-none')    }    // edit contact    close_edit_btn.click(() => {        disableEdit()    })    function disableEdit() {        close_edit_btn.addClass('d-none')        edit_contact_img_btn.addClass('d-none')        edit_contact_save_btn.addClass('d-none')        edit_contact_bottom.addClass('d-none')        close_person_btn.removeClass('d-none')        person_btn_div.removeClass('d-none')        to_contact_div.removeClass('d-none')        person_bottom.removeClass('d-none')    }    // new contact photo div open ---------    edit_contact_img_btn.click(() => {        $('#upload-new-photo-div').removeClass('d-none')    })    let url = document.URL;    if(url.split('&')[1]) {        let edit = url.split('&')[1].split('=')[1]        if (edit) activeEdit();    }    if ($('#error-check').val() === '1') {        activeEdit();    }})