$(document).ready(function () {    // for popover - bootstrap    let popoverTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="popover"]'));    popoverTriggerList.map(function (popoverTriggerEl) {        return new bootstrap.Popover(popoverTriggerEl)    })    // body - section    // for toggle main-menu    $('#main-menu-toggle-btn').click(() => {        $('.main-menu-section').toggleClass('toggle')        $('.main-body-div').toggleClass('toggle')    })    $('input').change(function() {        if ($(this).val() !== "") {            $(this).addClass("toggle")        }else {            $(this).removeClass("toggle")        }        console.log()    })    // navigation (main-menu)    let current_url = window.location.href    let sub_menu_aTaps = document.querySelectorAll('.sub-menu-container a')    sub_menu_aTaps.forEach(sub_menu_aTap => {        if (current_url === sub_menu_aTap.attributes.href.value) {            sub_menu_aTap.parentElement.classList.add('active');        }    })})